import 'package:flutter/material.dart';

class SoilTemperaturePage extends StatelessWidget {
  const SoilTemperaturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Soil Temperature')),
      body: const Center(child: Text('')),
    );
  }
}
